<h1>Stories</h1>
<div class="row">
    {{#each stories}}

    <div class="col s12 m4">
        <div class="card">
            <div class="card-image">
                {{! -- potential change to editIcon --}}
            </div>
            <div class="card-content center-aligned">
                <h5>{{title}}</h5>
                <p>{{body}}</p> {{! Will want a helper here to make the body not so long and strip paragraph tags. }}
                <br>
                <div class="chip"> {{! Pill shaped thing showing the image and name on stories}}
                    <img src="{{user.image}}" alt="">
                    <a href="/stories/user/{{user._id}}">{{user.displayName}}</a>
                </div>
            </div>
            <div class="card-action center-align">
                <a href="/stories/{{_id}}" class="btn grey">Read More</a> {{! Read More Button Creation and Link}}
            </div>
        </div>
    </div>

    {{else}}
    
    <p>No stories to display</p>

    {{/each}}
</div>